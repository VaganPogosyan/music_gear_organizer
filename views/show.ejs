<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Page</title>
</head>

<body>
    <h1>Your Item</h1>
    Name: <%= gear.name %> <br>
        Type: <%= gear.type %> <br>
            Color: <%= gear.color %> <br>
                Description: <%= gear.description %> <br>
                    <% if (gear.needsReplacement===true) { %>
                        This item needs fixing or replacement.
                        <% } else { %>
                            This item is in good condition.
                            <% } %>
                                <br>
                                <img src="<%= gear.image %>" alt="<%= gear.name %>"> <br>

                                <form action="/gear/<%= gear.id %>?_method=DELETE" method="POST">
                                    <input type="submit" value="DELETE">
                                </form>

                                <a href="/gear">Go back to home page</a>




</body>

</html>